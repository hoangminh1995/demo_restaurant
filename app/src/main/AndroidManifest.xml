<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demorestaurant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".RestaurantApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoRestaurant">
        <activity
            android:name=".ui.listrestaurant.ListRestaurantActivity"
            android:label="@string/list_restaurant"
            android:theme="@style/Theme.DemoRestaurant.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.detailrestaurant.DetailRestaurantActivity"
            android:theme="@style/Theme.DemoRestaurant.NoActionBar">

        </activity>
    </application>

</manifest>